export type JamItem = {
  id: string;
  title: string;
  image: string;
  description: string;
};

export type Edition = {
  year: string;
  image: string;
  title?: string;
  description?: string;
};